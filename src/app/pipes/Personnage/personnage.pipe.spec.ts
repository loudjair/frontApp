import { PersonnagePipe } from './personnage.pipe';

describe('PersonnagePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonnagePipe();
    expect(pipe).toBeTruthy();
  });
});
